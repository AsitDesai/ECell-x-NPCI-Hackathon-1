// lib/screens/personal_qr.dart
import 'package:flutter/material.dart';

class PersonalQrScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Personal QR")),
      body: Center(
        child: Text("Welcome to the Personal QR Screen!"),
      ),
    );
  }
}
